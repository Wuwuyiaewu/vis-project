import type { Options } from 'node_modules/vis-network/declarations/network/Network.d.ts'

export const options: Options = {
    interaction: {
        hover: true // 啟用 hover 事件
    },
    manipulation: {
        enabled: true,
    },
}